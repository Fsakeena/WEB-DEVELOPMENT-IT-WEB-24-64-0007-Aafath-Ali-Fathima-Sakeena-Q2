@page
@model WebApplication2.Pages.AddCourse.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>course</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.course.name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.course.name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.course.LecturerName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.course.LecturerName)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.course?.CourseId">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
